require 'rails_helper'

RSpec.describe Store, type: :model do
  pending "add some examples to (or delete) #{__FILE__}"

  context 'full address' do
    it 'combines the address and postal code' do

    end
  end

  context 'import' do
    it 'makes the first row the header row' do

    end
    it 'creates a Hash with the header and each row' do

    end
    it 'creates a new store for each row' do

    end
    it 'saves each store' do
      
    end
  end

  context 'open spreadsheet' do
    it 'creates new CSV file when extension name is .tsv' do

    end
    it 'creates new CSV file when extension name is .csv' do

    end
    it 'raises an error when file extension is not .tsv or .csv'

    end
  end
end
